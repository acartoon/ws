extends ../includes/layout/layout.pug
block var
    include ../includes/_var
    - var headerImage = paramsPage.CASES.image;
    - var pageTitle = paramsPage.CASES.pageMainTitle;
    - var contentClassModifier = paramsPage.CASES.class
    - var pageMainTitleHide = paramsPage.CASES.isHideMainTitle;
    - var pageMainTitle = paramsPage.CASES.pageMainTitle;
    - var isContainer = true;



block header
    h1.page-header__title.page-title=paramsPage.CASES.header
block content
    +casesPage
